---
phase: 2
plan: 3
wave: 3
---

# Plan 2.3: Queue API Triggers

## Objective
Implement API endpoints acting as webhook and manual triggers to push jobs into the queues.

## Context
- .gsd/SPEC.md
- src/lib/queue/client.ts
- src/lib/queue/jobs.ts

## Tasks

<task type="auto">
  <name>Implement API Handlers</name>
  <files>
    src/app/api/webhooks/route.ts
    src/app/api/triggers/route.ts
  </files>
  <action>
    Create route handlers to dispatch jobs manually and via webhook.
    Validate incoming requests using Zod schemas from `jobs.ts`.
    Push validated payload to the relevant queue.
    Remember to avoid directly invoking agents in route handlers as per database.mdc rules.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>API routes exist and correctly add jobs to queues.</done>
</task>

## Success Criteria
- [ ] `src/app/api/webhooks/route.ts` is implemented.
- [ ] `src/app/api/triggers/route.ts` is implemented.
