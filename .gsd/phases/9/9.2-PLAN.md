---
phase: 9
plan: 2
wave: 1
---

# Plan 9.2: Advanced Scenario Matrix - Ground Truth Expansion

## Objective
Expand the eval system's ground truth to cover advanced compliance edge cases, double scenario count, and verify agent performance on complex corporate and high-risk profiles.

## Context
- src/evals/ground-truth.ts
- src/evals/run.ts
- .gsd/SPEC.md

## Tasks

<task type="auto">
  <name>Expand Ground Truth Matrix</name>
  <files>src/evals/ground-truth.ts</files>
  <action>
    Add 15 new scenarios (CLT-016 through CLT-030) covering:
    - Corporate clients missing signatory lists.
    - HNW clients with expiring Investment Policy Statements (IPS).
    - Multi-jurisdictional clients requiring extra AML verification.
    - Scenarios with conflicting document statuses.
  </action>
  <verify>npx tsx scripts/validate-ground-truth.ts</verify>
  <done>Scenario count is 30 and all scenarios have valid schema/expected outcomes.</done>
</task>

<task type="auto">
  <name>Enhance Eval Runner for Large Batches</name>
  <files>src/evals/run.ts</files>
  <action>
    Optimize the `runEvals` script to handle larger matrices without timeout.
    - Add batching support (process 5 scenarios at a time).
    - Ensure summaries are correctly aggregated across batches.
  </action>
  <verify>npx tsx src/evals/run.ts --batch-size 5</verify>
  <done>Eval run completes for all 30 scenarios successfully.</done>
</task>

<task type="auto">
  <name>Execution Testing: Scenario Coverage</name>
  <files>tests/simulation/eval.coverage.test.ts</files>
  <action>
    Create an execution test to verify that the eval runner correctly handles the new scenarios.
    - Mock agent responses for various new scenarios.
    - Verify that scorers (escalationStage, duplicateAction) correctly identify mismatches on corporate-specific rules.
  </action>
  <verify>npx vitest tests/simulation/eval.coverage.test.ts</verify>
  <done>Execution tests verify that the eval system correctly catches logic errors in the new advanced scenarios.</done>
</task>

## Success Criteria
- [ ] Ground truth matrix contains 30 comprehensive scenarios.
- [ ] Eval runner handles batching for larger scenario sets.
- [ ] Execution tests confirm that the eval system can catch subtle advisor/management escalation errors.
