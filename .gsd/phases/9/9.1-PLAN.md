---
phase: 9
plan: 1
wave: 1
---

# Plan 9.1: Rule Engine Expansion - Conditional & Multi-Doc Logic

## Objective
Extend the compliance registry and tools to handle complex, conditional document requirements (e.g., Corporate vs Individual) and enforce multi-document dependencies.

## Context
- .gsd/SPEC.md
- src/lib/documents/registry.ts
- src/tools/compliance/getDocumentComplianceStatus.ts
- src/lib/db/enums.ts

## Tasks

<task type="auto">
  <name>Extend Document Registry with Conditional Rules</name>
  <files>src/lib/documents/registry.ts</files>
  <action>
    Update the `DocumentRegistry` to support the `AccountType` filter. 
    - Define `REQUIRED_DOCUMENTS_BY_ACCOUNT_TYPE` mapping.
    - Add a helper `getRequiredDocs(accountType: AccountType)` to return the correct set of documents.
    - Ensure "Corporate" accounts require `ARTICLES_OF_INCORPORATION` and `AUTHORIZED_SIGNATORY_LIST` which are new document types.
  </action>
  <verify>npx tsx scripts/test-registry-rules.ts</verify>
  <done>Registry correctly returns 4 docs for INDIVIDUAL and 6 docs for CORPORATE.</done>
</task>

<task type="auto">
  <name>Implement Multi-Doc Dependency Tooling</name>
  <files>src/tools/compliance/getDocumentComplianceStatus.ts</files>
  <action>
    Modify `getDocumentComplianceStatus` to check for "Blocker" documents.
    - If a blpcker (e.g. Government ID) is missing, other minor document requests should be deprioritized or the vault status should be set to "HIGH_RISK_BLOCKER".
    - Update the output schema to include a `isBlocker` flag or `reasons` array that references other documents.
  </action>
  <verify>npx vitest tests/tools/compliance.documentCompliance.test.ts</verify>
  <done>Tool correctly identifies a missing ID as a blocker for further compliance actions.</done>
</task>

<task type="auto">
  <name>Execution Testing: Rule-Aware Scenarios</name>
  <files>tests/simulation/rule.execution.test.ts</files>
  <action>
    Create a new execution test specifically for Phase 9.1.
    - Simulate a Corporate client with missing corporate docs.
    - Verify that the Compliance Agent identifies the correct missing documents based on the new registry rules.
    - Verify that the agent correctly prioritizes a missing ID over a missing questionnaire.
  </action>
  <verify>npx vitest tests/simulation/rule.execution.test.ts</verify>
  <done>All 3 execution tests for conditional rules and prioritization pass with 100% coverage.</done>
</task>

## Success Criteria
- [ ] Document registry supports account-type based requirements.
- [ ] Compliance tool identifies document dependencies and blockers.
- [ ] Execution tests prove rules are correctly applied in a simulated run.
