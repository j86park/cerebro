---
phase: 9
plan: 3
wave: 1
---

# Plan 9.3: Compliance Scorecard & Rule Audit UI

## Objective
Build the frontend and API infrastructure to visualize vault compliance scores and rule audit trails, providing transparency into why an agent took a specific action.

## Context
- src/components/dashboard/ComplianceScorecard.tsx
- src/app/api/vaults/[clientId]/scorecard/route.ts
- src/lib/db/vault-service.ts

## Tasks

<task type="auto">
  <name>Implement Scorecard API</name>
  <files>src/app/api/vaults/[clientId]/scorecard/route.ts</files>
  <action>
    Create a new API route to compute a real-time compliance score (0-100%).
    - Calculate score based on: (Valid Docs / Total Required Docs) * 50 + (Action Resolution Speed) * 50.
    - Return a list of "Applied Rules" and their status (PASSED/FAILED).
  </action>
  <verify>curl localhost:3000/api/vaults/CLT-003/scorecard</verify>
  <done>API returns exact score and rule breakdown for any valid clientId.</done>
</task>

<task type="auto">
  <name>Build Compliance Scorecard Component</name>
  <files>src/components/dashboard/ComplianceScorecard.tsx</files>
  <action>
    Implement a visual scorecard component using Recharts (radial gauge) and a detail list.
    - Show the 0-100 score prominently.
    - Display "Top Blocker" if compliance is below 70%.
    - List rules like "Government ID Present", "KYC Up to Date", etc.
  </action>
  <verify>npx vitest tests/components/ComplianceScorecard.test.tsx</verify>
  <done>Component renders correctly for both healthy and failing compliance states.</done>
</task>

<task type="auto">
  <name>Execution Testing: UI Data Integration</name>
  <files>tests/simulation/ui.execution.test.ts</files>
  <action>
    Create an execution test to verify that the dashboard correctly reflects the results of a simulation run through the scorecard.
    - Mock a simulation run completion.
    - Verify that the Scorecard component displays the metrics generated by the simulation.
    - Verify that "Blocker" rules triggered in simulation are highlighted in the UI.
  </action>
  <verify>npx vitest tests/simulation/ui.execution.test.ts</verify>
  <done>Execution tests prove that the UI layer correctly interprets and displays simulation-generated compliance health.</done>
</task>

## Success Criteria
- [ ] Scorecard API provides real-time rule audit and scoring.
- [ ] ComplianceScorecard UI is integrated into the dashboard.
- [ ] Execution tests verify end-to-end data flow from simulation to visualization.
