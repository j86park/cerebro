# Phase 8.1 Plan - Simulation Registry & Orchestration

## Goal
Establish the foundational infrastructure for simulation management and API control.

## Tasks
1. **Model Validation**: Ensure `SimulationRun` and `SimulationStatus` in `schema.prisma` are ready (Completed).
2. **Orchestrator Foundation**:
   - Create `src/lib/simulation/orchestrator.ts`.
   - Implement `createSimulationRun` and `updateProgress` methods.
3. **API Control Layer**:
   - Create `/api/simulation/start` route.
   - Implement POST handler that initializes the orchestrator and starts the first batch.

## Tests to Implement
1. **Unit Test**: `tests/simulation/orchestrator.lifecycle.test.ts`
   - Test `createSimulationRun` generates valid cuid and PENDING status.
   - Test `updateProgress` correctly increments `batchesCompleted` and handles `COMPLETED` transition.
2. **Integration Test**: `tests/api/simulation.start.test.ts`
   - Mock Prisma and verify API returns 201 on valid payload.
   - Verify 400 error on invalid parameters (e.g. negative client count).

## Verification
- API request to `/api/simulation/start` creates a record in the database with `PENDING` status.
- `npx vitest tests/simulation/orchestrator.lifecycle.test.ts` passes with 100% coverage on new logic.
