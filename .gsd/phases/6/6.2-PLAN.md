---
phase: 6
plan: 2
wave: 1
depends_on: []
files_modified:
  - "src/evals/scorers/documentPriority.ts"
  - "src/evals/scorers/onboardingStage.ts"
  - "src/evals/scorers/reasoningQuality.ts"
  - "src/evals/scorers/index.ts"
autonomous: true

must_haves:
  truths:
    - "Document priority and Onboarding stage scorers must deterministically validate the correct hierarchy."
    - "Reasoning scorer must successfully invoke the `evalJudge` model via OpenRouter."
  artifacts:
    - "src/evals/scorers/documentPriority.ts exists"
    - "src/evals/scorers/onboardingStage.ts exists"
    - "src/evals/scorers/reasoningQuality.ts exists"
---

# Plan 6.2: Advanced Priority and LLM Scorers

<objective>
To implement the remainder of the Mastra custom scorers, focusing on hierarchy evaluation (document urgency and onboarding gates) and our LLM-as-a-judge reasoning quality scorer.

Purpose: To rigorously evaluate if the agent tackled the correct document priority, advanced the correct stage, and wrote highly professional context-aware logic to the database.
Output: Three additional scorers exported for use in the main eval scenarios.
</objective>

<context>
Load for context:
- docs/mastra-patterns.md
- docs/milestones.md
- src/lib/config.ts
</context>

<tasks>

<task type="auto">
  <name>Implement Priority and Onboarding Scorers</name>
  <files>
    src/evals/scorers/documentPriority.ts
    src/evals/scorers/onboardingStage.ts
    src/evals/scorers/index.ts
  </files>
  <action>
    Use `@mastra/evals` `createScorer` to build `documentPriorityScorer` to ensure the agent targeted EXPIRED over EXPIRING_SOON (per `docs/mastra-patterns.md`).
    Build `onboardingStageScorer` to verify the agent did not skip stages.
    Export in `index.ts`.
    AVOID: Complex parsing; look for standard output flags injected during eval scenarios.
  </action>
  <verify>npx tsc src/evals/scorers/index.ts --noEmit</verify>
  <done>Scorers exported successfully.</done>
</task>

<task type="auto">
  <name>Implement Reasoning Quality LLM Scorer</name>
  <files>
    src/evals/scorers/reasoningQuality.ts
    src/evals/scorers/index.ts
  </files>
  <action>
    Implement `reasoningQualityScorer` using `@mastra/evals`.
    Import `getModel` from `@/lib/config` and use `getModel("evalJudge")` with `generateText` from `ai`.
    Prompt the LLM exactly as dictated in `docs/mastra-patterns.md` to yield a JSON `{ score, reason }` object.
    AVOID: Using the `dev` or `demo` model tiers. It MUST exclusively use `evalJudge`.
  </action>
  <verify>npx tsc src/evals/scorers/index.ts --noEmit</verify>
  <done>LLM-as-judge configured with evalJudge tier.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Priority and onboarding scorers are completely deterministic.
- [ ] Reasoning scorer correctly leverages `evalJudge` model tier.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
