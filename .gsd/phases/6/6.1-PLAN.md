---
phase: 6
plan: 1
wave: 1
depends_on: []
files_modified:
  - "src/evals/ground-truth.ts"
  - "src/evals/scorers/escalationStage.ts"
  - "src/evals/scorers/duplicateAction.ts"
  - "src/evals/scorers/index.ts"
autonomous: true

must_haves:
  truths:
    - "Ground truth mock data perfectly maps to our 15 database client scenarios."
    - "Rule-based scorers return deterministic 0.0 or 1.0 against strict enums."
  artifacts:
    - "src/evals/ground-truth.ts exists"
    - "src/evals/scorers/escalationStage.ts exists"
    - "src/evals/scorers/duplicateAction.ts exists"
---

# Plan 6.1: Eval Foundation and Rule Scorers

<objective>
To establish the ground truth evaluation targets for our 15 mock scenarios, and build the first pair of deterministic Mastra scorers (Escalation Stage and Duplicate Action).

Purpose: We need definitive correct behaviors for the agents to be validated against before constructing the LLM evals or full suite.
Output: The `ground-truth.ts` file housing expected outputs and our first deterministic scorers.
</objective>

<context>
Load for context:
- docs/data-schema.md
- docs/mastra-patterns.md
- docs/milestones.md
</context>

<tasks>

<task type="auto">
  <name>Create Ground Truth Definitions</name>
  <files>src/evals/ground-truth.ts</files>
  <action>
    Export an array or dictionary mapping the 15 mock clients (CLT-001 to CLT-015) from data-schema.md to their expected correct agent actions.
    Include expected `actionTaken`, `escalationStage`, `duplicateAction` (boolean), `highestPriority`, and `onboardingStage`.
    AVOID: Using dynamic dates; keep expectations relative to the documented `DEMO_DATE` scenarios.
  </action>
  <verify>npx tsc src/evals/ground-truth.ts --noEmit</verify>
  <done>Ground truth file exists with all 15 clients covered by strict typings.</done>
</task>

<task type="auto">
  <name>Implement Escalation and Duplicate Scorers</name>
  <files>
    src/evals/scorers/escalationStage.ts
    src/evals/scorers/duplicateAction.ts
    src/evals/scorers/index.ts
  </files>
  <action>
    Use `@mastra/evals` `createScorer` to build `escalationStageScorer` (checks if the agent chose the correct `NOTIFY_ADVISOR`, `ESCALATE_COMPLIANCE`, etc based on `expected.escalationStage`).
    Build `duplicateActionScorer` (checks if `expected.duplicateAction` matches the output's duplicate detection flag).
    Export them in `index.ts`.
    AVOID: Using LLM generation for these; they should purely parse `output` and compare to `expected`.
  </action>
  <verify>npx tsc src/evals/scorers/index.ts --noEmit</verify>
  <done>Scorers return 1.0 or 0.0 correctly with explanation strings based on deterministic logic.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Ground truth module encapsulates all mock client expectations.
- [ ] Rule-based scorers exist and do not invoke OpenRouter (`generateText`).
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
