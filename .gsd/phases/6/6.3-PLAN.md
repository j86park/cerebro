---
phase: 6
plan: 3
wave: 2
depends_on: ["6.1", "6.2"]
files_modified:
  - "src/evals/scenarios/compliance.eval.ts"
  - "src/evals/scenarios/onboarding.eval.ts"
autonomous: true

must_haves:
  truths:
    - "Both `complianceEvals` and `onboardingEvals` correctly import and use their relevant Mastra Agents."
    - "Scenarios pass the correct client IDs as `resourceId` and `threadId` to ensure isolated memory hits."
  artifacts:
    - "src/evals/scenarios/compliance.eval.ts exists"
    - "src/evals/scenarios/onboarding.eval.ts exists"
---

# Plan 6.3: Agent Eval Scenarios Definition

<objective>
To wire our ground truth test cases up to the `Mastra` `Eval` instances for testing both Compliance and Onboarding agents explicitly.

Purpose: To create the actual evaluation suites that trigger the LLM to run over our mock clients using its attached tools, producing an output we can grade.
Output: The Mastra `Eval` instances holding all our predefined test cases.
</objective>

<context>
Load for context:
- docs/mastra-patterns.md
- src/evals/ground-truth.ts (from Plan 6.1)
- src/evals/scorers/index.ts (from Plan 6.1 & 6.2)
</context>

<tasks>

<task type="auto">
  <name>Build Compliance Eval Suite</name>
  <files>src/evals/scenarios/compliance.eval.ts</files>
  <action>
    Import `Eval` from `@mastra/evals`. Attach `complianceAgent` and the compliance-appropriate scorers.
    Map the compliance-specific clients from `ground-truth.ts` into the `testCases` array.
    Ensure `resourceId` and `threadId` equal the `clientId` exactly.
    AVOID: Mixing onboarding scenarios into the compliance eval.
  </action>
  <verify>npx tsc src/evals/scenarios/compliance.eval.ts --noEmit</verify>
  <done>Compliance Eval instance is defined and heavily typed.</done>
</task>

<task type="auto">
  <name>Build Onboarding Eval Suite</name>
  <files>src/evals/scenarios/onboarding.eval.ts</files>
  <action>
    Import `Eval` from `@mastra/evals`. Attach `onboardingAgent` and onboarding-appropriate scorers.
    Map the onboarding-specific clients (e.g. CLT-001, CLT-004) from `ground-truth.ts` to test cases.
    Ensure `resourceId` and `threadId` targeting matches `clientId`.
    AVOID: Including compliance clients where the onboarding agent would do nothing.
  </action>
  <verify>npx tsc src/evals/scenarios/onboarding.eval.ts --noEmit</verify>
  <done>Onboarding Eval instance is defined with isolated scenarios.</done>
</task>

</tasks>

<verification>
After all tasks, verify:
- [ ] Evals properly link isolated client scenarios directly to the Mastra instances.
- [ ] 15 test scenarios effectively split between the two domain agents.
</verification>

<success_criteria>
- [ ] All tasks verified
- [ ] Must-haves confirmed
</success_criteria>
