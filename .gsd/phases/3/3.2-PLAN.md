---
phase: 3
plan: 2
wave: 1
---

# Plan 3.2: Onboarding Agent Definitions

## Objective
Define the Onboarding Agent, its system prompts, and memory schemas mimicking the strict patterns used for the Compliance Agent.

## Context
- .gsd/SPEC.md
- docs/mastra-patterns.md
- src/lib/config.ts

## Tasks

<task type="auto">
  <name>Create Onboarding Prompts and Memory Schemas</name>
  <files>
    src/agents/onboarding/prompts.ts
    src/agents/onboarding/memory-schema.ts
  </files>
  <action>
    Create `prompts.ts` exporting `ONBOARDING_SYSTEM_PROMPT` containing the 4 onboarding stages and document request rules exactly as documented in section 8 of `mastra-patterns.md`.
    Create `memory-schema.ts` exporting `ONBOARDING_WORKING_MEMORY_SCHEMA` using Zod exactly as documented in section 6 of `mastra-patterns.md`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Files export prompts and valid Zod schemas without type errors.</done>
</task>

<task type="auto">
  <name>Create Onboarding Agent Instance</name>
  <files>
    src/agents/onboarding/agent.ts
    src/agents/onboarding/index.ts
  </files>
  <action>
    Create the `onboardingAgent` using `@mastra/core` and `@mastra/memory`'s `createMemory`.
    Configure `model: getModel("dev")` resolving from `src/lib/config.ts`.
    Include placeholders for tools (implemented in Phase 4).
    Export the agent from `index.ts`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Agent is initialized correctly, utilizing the correct system prompt.</done>
</task>

## Success Criteria
- [ ] `prompts.ts` and `memory-schema.ts` created for onboarding.
- [ ] `agent.ts` properly constructs `onboardingAgent` with memory and config models.
