---
phase: 3
plan: 1
wave: 1
---

# Plan 3.1: Compliance Agent Definitions

## Objective
Define the Compliance Agent, its system prompts, and memory schemas according to the strict patterns defined in `docs/mastra-patterns.md`.

## Context
- .gsd/SPEC.md
- docs/mastra-patterns.md
- src/lib/config.ts

## Tasks

<task type="auto">
  <name>Create Compliance Prompts and Memory Schemas</name>
  <files>
    src/agents/compliance/prompts.ts
    src/agents/compliance/memory-schema.ts
  </files>
  <action>
    Create `prompts.ts` exporting `COMPLIANCE_SYSTEM_PROMPT` containing the 5-stage escalation ladder and priority urgencies exactly as documented in section 8 of `mastra-patterns.md`.
    Create `memory-schema.ts` exporting `COMPLIANCE_WORKING_MEMORY_SCHEMA` using Zod exactly as documented in section 6 of `mastra-patterns.md`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Files export prompts and valid Zod schemas without type errors.</done>
</task>

<task type="auto">
  <name>Create Compliance Agent Instance</name>
  <files>
    src/agents/compliance/agent.ts
    src/agents/compliance/index.ts
  </files>
  <action>
    Create the `complianceAgent` using `@mastra/core` and `@mastra/memory`'s `createMemory`.
    Configure `model: getModel("dev")` resolving from `src/lib/config.ts`.
    Include placeholders for the tools (which will be implemented in Phase 4).
    Export the agent from `index.ts`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Agent is initialized exactly per mastra-patterns.md section 2.</done>
</task>

## Success Criteria
- [ ] `prompts.ts` and `memory-schema.ts` created for compliance.
- [ ] `agent.ts` properly constructs `complianceAgent` with model resolution and memory configuration.
