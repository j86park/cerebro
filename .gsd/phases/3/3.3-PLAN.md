---
phase: 3
plan: 3
wave: 2
---

# Plan 3.3: Mastra Root Instance

## Objective
Initialize the single, global Mastra instance configured with Postgres memory storage. Wait until both agents (Compliance and Onboarding) exist before building this file (hence Wave 2).

## Context
- docs/mastra-patterns.md
- src/agents/compliance/agent.ts
- src/agents/onboarding/agent.ts
- src/lib/config.ts

## Tasks

<task type="auto">
  <name>Create Global Mastra Instance</name>
  <files>src/agents/mastra.ts</files>
  <action>
    Import `complianceAgent` and `onboardingAgent`.
    Initialize the `Mastra` core instance with those agents.
    Configure `storage` utilizing `PostgresStore` from `@mastra/memory/stores/postgres` with the `DATABASE_URL` from `env` configuration as detailed in section 1 of `mastra-patterns.md`.
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>The single Mastra instance is exported and compiles cleanly.</done>
</task>

## Success Criteria
- [ ] `src/agents/mastra.ts` exports `cerebro` using `PostgresStore`.
