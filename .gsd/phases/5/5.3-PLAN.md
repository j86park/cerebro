---
phase: 5
plan: 3
wave: 2
---

# Plan 5.3: Vault Grid (Operations Dashboard)

## Objective
Build the main `/dashboard` page showing all client vaults in a sortable grid with urgency indicators, onboarding status, and live document counts.

## Context
- GET /api/vaults → vault list data
- src/app/api/vaults/route.ts (Plan 5.1)
- src/components/layout/* (Plan 5.2)

## Tasks

<task type="auto">
  <name>Create dashboard page route</name>
  <files>src/app/dashboard/page.tsx</files>
  <action>
    Server component that fetches vault list.
    - Fetch GET /api/vaults on the server side (direct Prisma query fine for RSC)
    - Pass data to `<VaultGrid>` client component
    - Show header with firm summary stats: total clients, clients with issues, highest urgency level
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Dashboard page renders with data.</done>
</task>

<task type="auto">
  <name>Create VaultGrid component</name>
  <files>src/components/dashboard/VaultGrid.tsx</files>
  <action>
    Client component showing all vaults.
    - Table layout with columns: Client, Advisor, Account Type, Onboarding Status, Issues (counts for EXPIRED/MISSING/EXPIRING_SOON), Urgency badge, Last Action date, Trigger button
    - Urgency badge colors: CRITICAL=red, HIGH=amber, MEDIUM=yellow, LOW=blue, NONE=green
    - Clickable row → navigates to `/dashboard/vaults/[clientId]`
    - Sort by urgency by default (CRITICAL first)
    - Trigger button opens a confirm dialog then calls POST /api/agents/trigger
  </action>
  <verify>Visual inspection on dev server</verify>
  <done>Vault grid renders all clients with urgency sort and trigger button.</done>
</task>

<task type="auto">
  <name>Create UrgencyBadge and StatusBadge shared components</name>
  <files>src/components/ui-extensions/UrgencyBadge.tsx, src/components/ui-extensions/StatusBadge.tsx</files>
  <action>
    Reusable badge components wrapping shadcn/ui Badge.
    - `UrgencyBadge`: maps urgency string → variant + label + icon
    - `StatusBadge`: maps document/onboarding status → variant + label
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Badge components compile and render correctly.</done>
</task>

<task type="auto">
  <name>Create FirmSummaryBar component</name>
  <files>src/components/dashboard/FirmSummaryBar.tsx</files>
  <action>
    Header bar above the vault grid showing firm-wide health metrics.
    - Total vaults, clients with CRITICAL issues, clients with any issue, all-clear count
    - Compliance score as a percentage (VALID docs / total docs)
    - Uses shadcn Card components in a horizontal strip
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Firm summary bar renders with calculated metrics.</done>
</task>

## Success Criteria
- [ ] `/dashboard` renders all clients sorted by urgency.
- [ ] Urgency badges use correct color coding.
- [ ] Trigger button enqueues an agent run.
- [ ] Clicking a vault row navigates to the detail page.
