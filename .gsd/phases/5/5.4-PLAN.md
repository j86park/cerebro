---
phase: 5
plan: 4
wave: 2
---

# Plan 5.4: Vault Detail Page

## Objective
Build the `/dashboard/vaults/[clientId]` page showing a single client's full vault — documents, action history, and manual trigger controls.

## Context
- GET /api/vaults/[clientId] → full vault data
- POST /api/vaults/[clientId]/upload → mock upload
- Plan 5.1 (API routes), Plan 5.2 (design system)

## Tasks

<task type="auto">
  <name>Create vault detail page route</name>
  <files>src/app/dashboard/vaults/[clientId]/page.tsx</files>
  <action>
    Server component fetching full vault detail.
    - Fetch client profile, documents, and recent actions from the GET /api/vaults/[clientId] API
    - Render: ClientProfileCard + DocumentsTable + ActionHistoryFeed side by side (two-column layout)
    - Back link to /dashboard
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Vault detail page renders with correct data.</done>
</task>

<task type="auto">
  <name>Create ClientProfileCard component</name>
  <files>src/components/vault/ClientProfileCard.tsx</files>
  <action>
    shadcn Card showing client info.
    - Name, email, account type, advisor name, firm name
    - Onboarding stage + status with a visual stage progress bar (Stage 1→2→3→4)
    - Trigger agent run buttons (Compliance / Onboarding) with per-agent icons
    - Mock document upload button (opens dialog)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Profile card renders with trigger buttons.</done>
</task>

<task type="auto">
  <name>Create DocumentsTable component</name>
  <files>src/components/vault/DocumentsTable.tsx</files>
  <action>
    Full document registry table for the client.
    - Columns: Document Type, Category, Status badge, Expiry Date, Days Until Expiry, Notification Count
    - Rows colored by urgency level
    - Computed days until expiry from DEMO_DATE (fetch this from env or API response)
    - Sort by urgency default (EXPIRED first)
    - Uses shadcn Table component
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Documents table renders all documents with correct urgency computation.</done>
</task>

<task type="auto">
  <name>Create ActionHistoryFeed component</name>
  <files>src/components/vault/ActionHistoryFeed.tsx</files>
  <action>
    Scrollable timeline of agent actions.
    - Each entry: agent type icon (Compliance/Onboarding), action type label, timestamp, trigger badge (SCHEDULED/EVENT/MANUAL), reasoning text (truncated with expand toggle)
    - SEEDED_HISTORY entries styled subtly different (dimmed)
    - Shows up to 20 entries with a "Load more" affordance
    - Uses Supabase Realtime subscription for live updates (INSERT on agent_actions for this clientId)
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Action feed renders with Realtime subscription hook.</done>
</task>

<task type="auto">
  <name>Create MockUploadDialog component</name>
  <files>src/components/vault/MockUploadDialog.tsx</files>
  <action>
    Modal dialog for simulating a document upload.
    - Select document type from a dropdown (all DocumentType enum values)
    - Optional fileRef text field
    - Submit → POST /api/vaults/[clientId]/upload
    - Show success toast and trigger optimistic UI refresh
    - Uses shadcn Dialog + Select components
  </action>
  <verify>npx tsc --noEmit</verify>
  <done>Upload dialog submits and refreshes document table.</done>
</task>

## Success Criteria
- [ ] Vault detail page renders with documents and action history.
- [ ] Urgency colors match vault grid.
- [ ] Mock upload dialog triggers API and shows feedback.
- [ ] Action feed receives live updates via Supabase Realtime.
